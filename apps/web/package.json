{
  "name": "@repo/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "PORT=${WEB_PORT:-8000} next dev",
    "build": "next build",
    "start": "PORT=${WEB_PORT:-8000} next start",
    "lint": "biome check ."
  },
  "dependencies": {
    "@repo/api-client": "workspace:*",
    "@repo/ui": "workspace:*",
    "next": "^16",
    "react": "^19",
    "react-dom": "^19"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@tailwindcss/postcss": "^4",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
